// Placeholder — full implementation arrives in Phase 4 (financial budgets).
// Returns null until financialBudgets table + useBudgetVsActual hook exist.

export function BudgetWidget({ orgId: _orgId }: { orgId: string }) {
  return null
}
